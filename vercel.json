{
  "buildCommand": "npm run build && node build-static.js",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/gallery", "destination": "/gallery.html" }
  ]
}
